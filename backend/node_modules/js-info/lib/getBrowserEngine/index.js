'use strict';

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = getBrowserEngine;

var _get = require('lodash/get');

var _get2 = _interopRequireDefault(_get);

var _ = require('..');

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function getBrowserEngine() {
  if (_.browser.value === 'na') return 'na';
  if (typeof window === 'undefined') return 'unknown';
  var userAgent = (0, _get2.default)(window, 'navigator.userAgent', '');
  if (_.os.ios) return 'webkit';
  if ((_.browser.chrome || _.browser.opera || _.os.android && _.browser.edge) && !!window.CSS) {
    return 'blink';
  }
  if (_.browser.safari) return 'webkit';
  if (_.browser.ie) return 'trident';
  if (_.browser.edge) return 'edgehtml';
  if (_.browser.firefox || /gecko/i.test(userAgent)) return 'gecko';
  if (/webkit/i.test(userAgent)) return 'webkit';
  return 'unknown';
}